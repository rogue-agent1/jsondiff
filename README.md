# jsondiff
Structural JSON diff with colored output.
```bash
python jsondiff.py old.json new.json
python jsondiff.py config.json config.prod.json --stats
python jsondiff.py a.json b.json -j
```
## Zero dependencies. Python 3.6+.
